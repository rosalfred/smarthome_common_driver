//force project name
//this is needed because repository contains multipackage and cause problem with release
rootProject.name = 'common_driver'

include '..:network_zeroconf',
    '..:rosjava_dynamic_reconfigure'